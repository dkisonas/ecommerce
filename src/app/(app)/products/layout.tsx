// TailwindPlus styled products layout
// Filters are now built into the products page itself

import React from 'react'

export default function ShopLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
